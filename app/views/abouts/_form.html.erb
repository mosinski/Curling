<%= tinymce_assets %>
<%= form_for(@about) do |f| %>
  <% if @about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% @about.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b><%= f.label "O klubie po Polsku" %></b>
    <%= f.text_area :tekst_pl, :class => "tinymce", :rows => 10, :cols => 12 %>
  </div>
  <br />
  <div class="field">
    <b><%= f.label "O klubie po Angielsku" %></b>
    <%= f.text_area :tekst_en, :class => "tinymce", :rows => 10, :cols => 12 %>
  </div>
  <div class="actions">
    <button type="submit" class="btn btn-primary">
	<i class="icon-check"></i> Aktualizuj Opis Klubowy
    </button>
  </div>
<% end %>
<%= tinymce :language => "pl", :entity_encoding => "raw" %>
