<h3><i class="icon-tags"></i> Dodaj Nową Drużyne do Bazy</h3>

<%= form_for @teams_email, :html => { :multipart => true } do |f| %>
<%= render partial: 'form', locals: { f: f } %>
<br />
<div class="actions">
    <button type="submit" class="btn btn-primary">
	<i class="icon-check"></i> Dodaj Drużynę
    </button>
  </div>
<% end %>
