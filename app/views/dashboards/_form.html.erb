  <% if @dashboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dashboard.errors.count, "error") %> prohibited this dashboard from being saved:</h2>

      <ul>
      <% @dashboard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :tytul, :placeholder => "Tytuł Aktualności Klubowej", "style" => "width:100%" %>
  </div>

  <div class="field">
    <%= f.text_area :tekst, :class => "redactor", :rows => 100, :cols => 120 %>
  </div>
