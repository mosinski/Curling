<h3>Nowa Notatka</h3>

<%= form_for @dashboard, :html => { :multipart => true } do |f| %>
<%= render partial: 'form', locals: { f: f } %>
<br />
<div class="actions">
     <button type="submit" class="btn btn-primary">
  	<i class="icon-check"></i> Stwórz Notatkę
     </button>
</div>
<% end %>
